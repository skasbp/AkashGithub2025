pipeline {
    agent { label 'SLAVE2'}
    stages {
        stage('SLAVE 1') {
            steps {
                sh '''
                   ls -lart
                   pwd
                   env
                   sleep 2
                '''
            }
        }
        stage('SLAVE 2') {  
             steps{
                sh '''
                   sleep 5
                '''
             }
        }
    }
}