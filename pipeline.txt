pipeline {
    agent { label 'SLAVE1'}
    stages{
        stage ('Stages1') {
            step{
                sh 'echo "Welcome t0 my world"'
                sh 'ls -lart'
                sh 'cat README.md'
            }
        }
        stages ('Stages2'){
            step {
                sh 'echo "it is a stages 2 phase"'
            }
        }
        stage ('Stages3){
            agent { label 'SLAVE 2'}
            step {
                sh '
            }
        }
    }
}